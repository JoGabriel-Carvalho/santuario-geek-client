import React from "react";

const Authentication = () => {

    return (
        <React.Fragment>
            <h4>Authentication</h4>
        </React.Fragment>
    );
};

export default Authentication;